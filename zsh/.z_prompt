# .zsh_prompt
# Author: Icko Iben
# Created on: Mon; 4 Sep 2023; 02:49
# Description: Customizes the zsh prompt appearance and functionalities.
# Source: https://medium.com/@charlesdobson/how-to-customize-your-macos-terminal-7cce5823006e
# Note: The layout for the prompt is 'name directory (git-branch) >> |'.

# ===================
# Utility Functions
# ===================
# Display current GIT branch (if inside a git repository)
git_branch() {
    git branch 2>/dev/null | sed -e '/^[^*]/d' -e 's/* \(.*\)/ (\1)/'
}

# ===================
# Prompt Customization
# ===================
# Define prompt appearance components
PROMPT="%{$fg[red]%}%n%{$reset_color%}"             # username
PROMPT+=" "                                        # space separator
PROMPT+="%{$fg[magenta]%}%1~%{$reset_color%}"      # current directory
PROMPT+="%{$fg[cyan]%}\$(git_branch)%{$reset_color%}"  # current git branch (if applicable)
PROMPT+=" "                                        # space separator

# Adjust prompt symbol based on user privileges
if [ "$UID" -ne 0 ]; then
    PROMPT+=">> "                                  # regular user
else
    PROMPT+="$ "                                   # superuser (root)
fi

# ===================
# Color Configurations
# ===================
# Enable color support (zsh typically supports color out-of-the-box)
# Set LSCOLORS to customize directory and file colors (same as bash in this case)
export LSCOLORS=ExFxBxDxCxegedabagacad

